# Weekly Retrospective — Week 1

**Covers:** 2026-07-12 → 2026-07-18 · **Sprint 1 (Validation & Data):** Zod → TanStack Query
**Written:** 2026-07-19

---

## 1. What did I learn this week?
A lot for a first week:
- **Zod:** basics (`z.infer` + `safeParse`) + advanced concepts (the coerce/preprocess/transform/pipe
  taxonomy, and that `.refine()` sits *outside* it — it validates, it doesn't massage).
- **TypeScript:** discriminated-union narrowing, `unknown` vs `any`, `||` vs `??`.
- **TanStack Query:** server vs client state, `queryKey` as cache address, the `queryFn` throw
  rule, `status` vs `fetchStatus`.

## 2. Where did I struggle most?
- The **type-vs-runtime line** kept needing restating (assertions, erased types, external data
  as `unknown`).
- **Process, not code:** the 2-day skip (07-16 / 07-17).

## 3. Which mistakes recurred?
- **"An assertion / declared type doesn't validate runtime data."** Appeared in the mistakes
  log three times (07-14 ×2, 07-15) — a genuine recurring theme, not a one-off.

## 4. What will I do differently next week?
- **Resume at a steady pace instead of cramming to "catch up"** — there's no calendar debt; the
  sprint schedule just shifts, the bar doesn't.

## 5. Which topic can I now explain without help?
- The Zod **`safeParse` → `z.infer`** loop, and **server vs client state**.

---

## Carry-forward
- **Turn the recurring mistake into a flashcard** (Q3): *"Does a TypeScript type/assertion
  validate runtime data?"* → No — types are erased at transpile; only a runtime check (`safeParse`)
  validates. This is the week's most-repeated gap; spaced repetition is the fix.
- **Steady pace, honestly dated** (Q4): one lesson done well beats three crammed; date entries
  live, the day they happen. (Date-drift across `journal.md`/`mistakes.md` was caught and fixed
  this week — watch it doesn't recur.)
- **Consolidation check (Q5):** say the `safeParse → z.infer` loop and server-vs-client aloud,
  no docs. If either stalls, it isn't consolidated yet — that's next week's review target.
